import { TimesDirDirective } from './times-dir.directive';

describe('TimesDirDirective', () => {
  it('should create an instance', () => {
    const directive = new TimesDirDirective();
    expect(directive).toBeTruthy();
  });
});
